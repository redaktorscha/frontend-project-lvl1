/**
 * returns missing elem in the given progression marked as '..'
 * @param {Array<number|string>} arr
 * @returns {number}
 */
const getMissingSeqElem = (arr) => arr.slice(-1);

export default getMissingSeqElem;
